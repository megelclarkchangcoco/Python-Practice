# nested loops  = the "inner loop" will finish all of it's iteration before
#                    finishing one iteration of the "outer loop"


# rows =int(input("How many Rows? :"))
# columns = int(input("How many Columns? :"))
# symbols = input("Enter Symbols? : ")
#
#
# for i in range(rows): # this for is for outer loop
#     for j in range(columns): # this for is for inner loop
#         print(symbols, end = "") # this line of code will excute why nested loop works and what user input symbol
#     print() # end of print
